#include <stdio.h>

int main(int argc, char** argv){
    int val, *p;
    val = 10;
    p = &val;
    printf("%d",*p);
    //char* ch;
    //ch = (argv++);
    //char chv = *ch;
    //printf(chv); 
    getchar();
}
